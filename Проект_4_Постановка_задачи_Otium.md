# 🎬 Постановка задачи разработчику — стриминговый сервис Otium

> Проект по системному анализу: от интервью с заказчиком до диаграммы последовательности, тестирования REST API и постановки задачи на разработку.

**Роль:** системный аналитик, лидер команды
**Команда:** «Команда Лето»
**Инструменты:** Swagger, draw.io, Google Docs
**Коммуникация:** Telegram — чат группы, Яндекс Телемост — созвоны

---

## О проекте

Заказчик Otium заметил, что пользователям сложно выбрать, что посмотреть: приходится сравнивать по десять фильмов, смотреть трейлеры, а часть пользователей просто уходит с платформы, не досмотрев до выбора.

**Решение** — добавить рейтинг от кинокритиков рядом с уже существующим зрительским рейтингом и дать возможность сортировать список по любому из них.

Из интервью с заказчиком выяснилась пара нюансов, которые сильно повлияли на итоговую задачу. Не у всех фильмов и сериалов есть оценка критиков — детский контент часто её не получает. Изначально предложили присваивать таким позициям рейтинг 0, но тогда при сортировке по возрастанию они оказывались бы в начале списка, что не имело смысла. Договорились на другом: фильмы и сериалы без рейтинга всегда уходят в конец списка, независимо от направления сортировки.

---

## Как была организована работа

Как лидер команды, я занимался распределением задач и синхронизацией команды.

- **Telegram** — основной канал общения: обсуждение вопросов по ходу работы, снятие блокеров.
- **Яндекс Телемост** — созвоны для решений, которые нужно было принимать вместе.

---

## Что сделано

### 🔍 Извлечение требований

Разобрал интервью с заказчиком и зафиксировал требование в чистом виде. До идеи с сортировкой заказчик просил только добавить рейтинг критиков — фильтрацию и сортировку по нему добавили уже в процессе обсуждения.

### 🔀 Диаграмма последовательности

Построил диаграмму последовательности для новой функции — как запрос на сортировку проходит от пользователя через веб-сервер к сервисам «Фильмы» и «Сериалы» и обратно.

### 🧪 Тестирование существующего API

Протестировал существующее REST API в Swagger — прошёлся по Web Server API, Films Server API и Series Server API. Посмотрел, какие параметры и поля уже есть, и сверил это с тем, что нужно для новой функции.

### 🛠️ Постановка задачи разработчику

По итогу тестирования стало понятно, что менять нужно во всех трёх API:

| API | Изменение |
|---|---|
| Films Server, Series Server | Добавить новое поле `criticsRating` — опциональное, так как есть не у всех позиций |
| Web Server | Принимать новый query-параметр `sortBy` — по какому рейтингу сортировать: зрительскому или критиков |
| Web Server | Принимать новый query-параметр `direction` — по возрастанию или убыванию |
| Web Server | Передавать `criticsRating` дальше в ответе — как в списке, так и в детальной карточке |

Оформил всё это тикетами на разработку для каждого из трёх API.

---

## Результат

📄 [Тикет на разработку с описанием изменений во всех трёх API](https://docs.google.com/document/d/1FE8baJdk-b9TxB3wE3mu1-B9k4MSjtjDGQnbN7wrxW8/edit)
🔀 [Диаграмма последовательности в draw.io](https://drive.google.com/file/d/18PaLyfzI2argWGNWNF_9qwaXHGUBsaZ-/view?usp=sharing)

---

## Что показывает этот проект

- извлечение требований из интервью с заказчиком с уточнением спорных моментов (поведение сортировки для позиций без данных);
- построение диаграммы последовательности;
- тестирование существующего REST API в Swagger;
- постановку задачи разработчику с точным описанием изменений на уровне полей и параметров запроса.
